# OSF v8 — Secrets (TEMPLATE — replace before deploy!)
# Create via:
#   kubectl create secret generic osf-secrets \
#     --from-literal=JWT_SECRET="$(openssl rand -base64 32)" \
#     --from-literal=DB_PASSWORD="$(openssl rand -base64 16)" \
#     --from-literal=NR_POD_SECRET="$(openssl rand -base64 32)" \
#     --from-literal=LLM_ENCRYPTION_KEY="$(openssl rand -base64 32)" \
#     --from-literal=FACTORY_DB_PASSWORD="Kohlgrub.123" \
#     --from-literal=ERP_DB_PASSWORD="Kohlgrub.123" \
#     --from-literal=QMS_DB_PASSWORD="Kohlgrub.123" \
#     --from-literal=ANTHROPIC_API_KEY="" \
#     --from-literal=LANGFUSE_SECRET_KEY="" \
#     --from-literal=LANGFUSE_PUBLIC_KEY="" \
#     -n osf --dry-run=client -o yaml | kubectl apply -f -
#
# NEVER commit plaintext secrets!
apiVersion: v1
kind: Secret
metadata:
  name: osf-secrets
  namespace: osf
type: Opaque
stringData:
  JWT_SECRET: "CHANGE_ME"
  DB_PASSWORD: "CHANGE_ME"
  NR_POD_SECRET: "CHANGE_ME"
  LLM_ENCRYPTION_KEY: "CHANGE_ME"
  FACTORY_DB_PASSWORD: "CHANGE_ME"
  ERP_DB_PASSWORD: "CHANGE_ME"
  QMS_DB_PASSWORD: "CHANGE_ME"
  ANTHROPIC_API_KEY: ""
  LANGFUSE_SECRET_KEY: ""
  LANGFUSE_PUBLIC_KEY: ""

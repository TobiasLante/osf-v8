# Create secret manually:
# kubectl create secret generic osf-secrets \
#   --from-literal=jwt-secret="$(openssl rand -base64 32)" \
#   --from-literal=db-password="$(openssl rand -base64 16)" \
#   -n demo
#
# This file is a template only — DO NOT commit real secrets!
apiVersion: v1
kind: Secret
metadata:
  name: osf-secrets
  namespace: demo
type: Opaque
stringData:
  jwt-secret: "CHANGE_ME"
  db-password: "CHANGE_ME"

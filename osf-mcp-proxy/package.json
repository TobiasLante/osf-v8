{
  "name": "osf-mcp-proxy",
  "version": "9.0.0",
  "description": "MCP Proxy — JSON-RPC forward proxy with governance filtering",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx swc src -d dist --source-maps --copy-files --strip-leading-paths",
    "dev": "npx tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@swc/cli": "^0.8.0",
    "@swc/core": "^1.15.0",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
